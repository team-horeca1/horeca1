# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# locally-uploaded vendor KYC documents (served from disk; prod uses a Docker volume)
/uploads

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

/generated/prisma

# Claude Code agent worktrees (ephemeral, should never be committed)
.claude/worktrees/

.git-commit-msg.tmp

# Audit disposable credentials (never commit)
AUDIT_SECRETS.local.md
audit-cookies*.txt
audit-*-cookies.txt
audit-retest-*.txt
audit-verify-*.txt
audit-session-token.txt
.playwright-mcp/
test-results/
/playwright-report/
tmp-*.json
*_probe.json
oversell*.json
login-resp.txt
login.html
register.html
me1.json
bad-login-body.txt
rice.json
tomato-search.json
